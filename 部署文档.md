# 三角洲平台 Docker 部署文档

本项目使用 Docker Compose 管理全部服务。用户不需要逐个下载、配置和启动容器，只要准备好本目录中的部署文件，修改 `.env` 后执行一条命令，即可自动拉取并启动全部五个服务。

## 一、部署后包含哪些服务

`docker-compose.yml` 定义了以下五个服务：

| 服务 | 镜像 | 容器名称 | 用途 |
| --- | --- | --- | --- |
| 后端 | `lenglengyou/delta-game:tagname` | `delta-backend` | Java/Spring Boot API |
| 管理后台 | `lenglengyou/delta-game-admin:tagname` | `delta-admin-ui` | PC 管理后台 |
| H5 | `lenglengyou/delta-game-h5:tagname` | `delta-h5` | H5 用户端 |
| MySQL | `mysql:8.0` | `delta-mysql` | 业务数据库 |
| Redis | `redis:7-alpine` | `delta-redis` | 缓存和会话数据 |

Docker Desktop 中显示的 `delta-game` 是 Compose 项目分组，不是单独的镜像。展开该分组后会看到以上五个容器。

## 二、准备部署目录

部署时将以下文件放在同一个目录中：

```text
部署目录/
├── docker-compose.yml
├── .env.example
└── delta_game.sql
```

这些文件的作用：

- `docker-compose.yml`：声明要拉取的五个镜像、容器名称、依赖关系、端口、环境变量、网络和数据卷。
- `.env.example`：配置模板，需要复制为 `.env` 后填写实际参数。
- `.env`：当前部署实例的真实配置，不会被打包进镜像。
- `delta_game.sql`：MySQL 第一次创建空数据卷时自动导入的初始化 SQL。

所有 Docker 命令都必须在该部署目录中执行，否则 Docker Compose 找不到 `docker-compose.yml` 和 `.env`。

## 三、创建并修改配置

### Windows PowerShell

```powershell
Copy-Item .env.example .env
```

### Linux

```bash
cp .env.example .env
```

打开 `.env`，至少修改以下内容：

```env
# MySQL
MYSQL_ROOT_PASSWORD=请填写强Root密码
MYSQL_USER=delta_game
MYSQL_PASSWORD=请填写强数据库密码

# Redis
REDIS_PASSWORD=请填写强Redis密码

# 后端安全配置
JWT_SECRET=请填写至少32字符的随机字符串
AES_KEY=请填写16字节AES密钥
AUTH_H5_LOGIN_CODE=123456

# 微信小程序，每个部署方填写自己的参数
WX_MINIAPP_ENABLED=true
WX_MINIAPP_APPID=你的微信小程序AppID
WX_MINIAPP_SECRET=你的微信小程序AppSecret

# 微信支付总开关，详细支付参数在管理后台配置
WX_PAY_ENABLED=false

# 文件和访问端口
FILE_UPLOAD_URL_PREFIX=/file/
ADMIN_PORT=8081
H5_PORT=8082
```

生产环境必须更换示例密码和密钥。`.env` 包含敏感配置，不要提交到 Git 仓库，也不要发送给无关人员。

## 四、配置文件如何连接到镜像

不需要修改镜像，也不需要把配置文件复制进镜像。配置传递过程如下：

```text
.env
  ↓ Docker Compose 自动读取
docker-compose.yml 中的 ${变量名}
  ↓ environment 注入
后端、MySQL、Redis 容器的环境变量
  ↓ Spring Boot 自动映射
数据库、Redis、JWT、AES、微信小程序等运行配置
```

主要映射关系：

| `.env` 配置 | 注入的容器变量 | 实际作用 |
| --- | --- | --- |
| `MYSQL_ROOT_PASSWORD` | `MYSQL_ROOT_PASSWORD` | MySQL Root 密码 |
| `MYSQL_USER` | `MYSQL_USER`、`SPRING_DATASOURCE_USERNAME` | MySQL 用户和后端数据库用户名 |
| `MYSQL_PASSWORD` | `MYSQL_PASSWORD`、`SPRING_DATASOURCE_PASSWORD` | MySQL 用户密码和后端数据库密码 |
| `REDIS_PASSWORD` | `SPRING_DATA_REDIS_PASSWORD`，同时传给 Redis 启动命令 | Redis 密码 |
| `JWT_SECRET` | `JWT_SECRET` | 后端 JWT 签名密钥 |
| `AES_KEY` | `AES_KEY` | 后端 AES 加密密钥 |
| `WX_MINIAPP_ENABLED` | `WX_MINIAPP_ENABLED` | 是否启用微信小程序能力 |
| `WX_MINIAPP_APPID` | `WX_MINIAPP_APPID` | Spring Boot 的 `wx.miniapp.appid` |
| `WX_MINIAPP_SECRET` | `WX_MINIAPP_SECRET` | Spring Boot 的 `wx.miniapp.secret` |
| `ADMIN_PORT` | 管理端端口映射 | 默认通过宿主机 `8081` 访问 |
| `H5_PORT` | H5 端口映射 | 默认通过宿主机 `8082` 访问 |

`docker-compose.yml` 已经写好这些连接关系。部署人员只需要修改同目录的 `.env`，然后通过 Compose 启动服务。

## 五、一条命令拉取并启动五个服务

如果 Docker Hub 仓库是私有仓库，第一次部署前先登录：

```bash
docker login
```

确认当前终端位于部署目录，然后执行：

```bash
docker compose up -d --pull always
```

这一条命令会依次完成：

1. 读取当前目录的 `docker-compose.yml`。
2. 自动读取同目录的 `.env`。
3. 拉取后端、管理后台、H5、MySQL 和 Redis 五个镜像。
4. 创建 `delta-game` 专用 Docker 网络。
5. 创建 MySQL、Redis 和上传文件的数据卷。
6. 启动 MySQL 和 Redis。
7. 等待 MySQL 健康检查通过。
8. 启动后端，再启动管理后台和 H5。
9. 以后台模式持续运行全部容器。

这是默认且唯一需要执行的部署命令，不需要分别执行 `docker pull`，也不需要手动逐个启动容器。`--pull always` 会检查五个镜像并拉取可用版本，Compose 随后自动注入 `.env` 配置并按依赖顺序启动全部服务。

## 六、检查运行状态

```bash
docker compose ps
```

正常情况下应看到以下五个容器处于运行状态：

```text
delta-mysql
delta-redis
delta-backend
delta-admin-ui
delta-h5
```

查看后端启动日志：

```bash
docker compose logs -f backend
```

启动完成后访问：

- 管理后台：`http://服务器IP:8081`
- H5 页面：`http://服务器IP:8082`

如果在 `.env` 中修改了 `ADMIN_PORT` 或 `H5_PORT`，访问时使用修改后的端口。

## 七、修改配置和更新镜像

修改 `.env` 后重新创建相关容器，后端才会读取新配置：

```bash
docker compose up -d --force-recreate backend
```

拉取最新镜像并更新全部服务：

```bash
docker compose up -d --pull always
```

停止并移除容器，但保留数据库、Redis 和上传文件数据：

```bash
docker compose down
```

不要执行 `docker compose down -v`，该命令会连同 MySQL、Redis 和上传文件的数据卷一起删除。

## 八、数据和微信支付配置

- `delta_game.sql` 只会在 MySQL 数据卷第一次创建且为空时自动导入。
- 上传文件、微信支付私钥和公钥保存在 `upload-data` 数据卷中。
- 微信支付 AppID、商户号、API v3 密钥、证书和回调地址在管理后台“系统配置 -> 微信支付配置”中填写。
- 微信小程序登录使用的 AppID/AppSecret 在 `.env` 中填写，修改后需重新创建后端容器。
