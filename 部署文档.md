# 三角洲平台 Docker 部署

本仓库只包含 Docker 配置文件。部署前先安装 Docker Desktop（Windows）或 Docker Engine 与 Docker Compose v2（Linux）。

## 1. 配置

将 `.env.example` 复制为 `.env`，并修改以下配置：

```env
MYSQL_ROOT_PASSWORD=强Root密码
MYSQL_PASSWORD=强数据库密码
REDIS_PASSWORD=强Redis密码
JWT_SECRET=至少32字符的随机字符串
AES_KEY=16字节AES密钥

WX_MINIAPP_ENABLED=true
WX_MINIAPP_APPID=你的微信小程序AppID
WX_MINIAPP_SECRET=你的微信小程序AppSecret

ADMIN_PORT=8081
H5_PORT=8082
FILE_UPLOAD_URL_PREFIX=/file/
```

Docker 配置文件说明：

- `.env`：当前实例的密码、密钥、小程序和端口配置。
- `docker-compose.yml`：镜像、容器、端口和数据卷定义。
- `delta_game.sql`：仅首次创建 MySQL 数据卷时导入的初始化数据。

## 2. 拉取镜像

私有仓库先登录：

```bash
docker login
```

拉取项目镜像：

```bash
docker pull lenglengyou/delta-game:tagname
docker pull lenglengyou/delta-game-admin:tagname
docker pull lenglengyou/delta-game-h5:tagname
docker pull mysql:8.0
docker pull redis:7-alpine
```

## 3. 启动服务

在本目录执行：

```bash
docker compose up -d
```

启动后访问：

- 管理后台：`http://服务器IP:8081`
- H5 页面：`http://服务器IP:8082`

修改 `.env` 中的小程序配置后重启后端：

```bash
docker compose up -d --force-recreate backend
```

查看状态和日志：

```bash
docker compose ps
docker compose logs -f backend
```

停止服务但保留数据：

```bash
docker compose down
```

微信支付参数在管理后台“系统配置 -> 微信支付配置”中填写。请勿执行 `docker compose down -v`，除非确认需要删除数据库、Redis 和上传文件。
