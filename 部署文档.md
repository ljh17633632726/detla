# 三角洲平台 Docker 部署

本仓库只包含 Docker Compose 配置文件。部署前安装 Docker Desktop（Windows）或 Docker Engine 与 Docker Compose v2（Linux），并在本目录打开终端。

## 1. 拉取镜像

拉取后端、管理后台、H5 与基础服务镜像：

```bash
docker pull lenglengyou/delta-game:tagname
docker pull lenglengyou/delta-game-admin:tagname
docker pull lenglengyou/delta-game-h5:tagname
docker pull mysql:8.0 ”已有数据库可以不安装“
docker pull redis:7-alpine
```

## 2. 创建并修改配置

配置模板删除后缀.example：

```bash
.env.example  -> .env 或者linux执行 cp .env.example .env
```

打开本目录的 `.env`，至少修改以下配置：

```env
MYSQL_ROOT_PASSWORD=数据库的Root密码
MYSQL_PASSWORD=数据库密码
REDIS_PASSWORD=Redis密码
JWT_SECRET=至少32字符的随机字符串”随便输入“
AES_KEY=16字节AES密钥”随便输入“

WX_MINIAPP_ENABLED=true
WX_MINIAPP_APPID=你的微信小程序AppID
WX_MINIAPP_SECRET=你的微信小程序AppSecret

ADMIN_PORT=8081
H5_PORT=8082
FILE_UPLOAD_URL_PREFIX=/file/
```

### 配置如何传入镜像

`.env` 不会打包进镜像。启动时 Docker Compose 读取同目录的 `.env`，再按 `docker-compose.yml` 的 `environment` 字段注入到容器：

| `.env` 字段 | `docker-compose.yml` 注入变量 | 容器内使用位置 |
| --- | --- | --- |
| `MYSQL_USER` / `MYSQL_PASSWORD` | `SPRING_DATASOURCE_USERNAME` / `SPRING_DATASOURCE_PASSWORD` | 后端数据库连接 |
| `REDIS_PASSWORD` | `SPRING_DATA_REDIS_PASSWORD` | 后端 Redis 连接 |
| `JWT_SECRET` | `JWT_SECRET` | Spring Boot `jwt.secret` |
| `AES_KEY` | `AES_KEY` | Spring Boot `aes.key` |
| `WX_MINIAPP_ENABLED` | `WX_MINIAPP_ENABLED` | Spring Boot `wx.miniapp.enabled` |
| `WX_MINIAPP_APPID` | `WX_MINIAPP_APPID` | Spring Boot `wx.miniapp.appid` |
| `WX_MINIAPP_SECRET` | `WX_MINIAPP_SECRET` | Spring Boot `wx.miniapp.secret` |
| `ADMIN_PORT` / `H5_PORT` | Compose 端口映射 | 管理后台 / H5 访问端口 |

`docker-compose.yml` 已把变量映射写好，只修改 `.env` 即可

## 3. 启动服务

```bash
docker compose up -d
```

启动后访问：

- 管理后台：`http://服务器IP:8081`
- H5 页面：`http://服务器IP:8082`

修改 `.env` 后，重新创建后端容器使新配置生效：

```bash
docker compose up -d --force-recreate backend
```

查看状态和日志：

```bash
docker compose ps
docker compose logs -f backend
```

停止服务但保留数据：

```bash
docker compose down
```

微信支付参数在管理后台“系统配置 -> 微信支付配置”中填写。首次启动会导入 `delta_game.sql`；已有 MySQL 数据卷不会重复导入。请勿执行 `docker compose down -v`，除非确认需要删除数据库、Redis 和上传文件。
