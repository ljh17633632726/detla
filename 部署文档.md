# 三角洲平台 Docker 一键部署

本项目通过 Docker Hub 拉取运行镜像，不提供也不需要离线 Docker 镜像包、Java JAR 或前端静态包。

## 配置目录

部署服务器上只需要保留本仓库中的文件。该目录就是 Docker 配置目录：

- `docker-compose.yml`：服务、镜像、端口和数据卷定义，通常无需修改。
- `.env`：当前部署实例的密码、密钥、微信小程序和端口配置，必须按实际情况修改。
- `.env.example`：`.env` 配置模板。
- `delta_game.sql`：仅在首次创建 MySQL 数据卷时初始化数据库。
- `start.bat` / `start.ps1`：Windows 自动拉取镜像并启动。
- `start.sh`：Linux 自动拉取镜像并启动。

## 首次部署

1. 安装 Docker Desktop（Windows）或 Docker Engine 与 Docker Compose v2（Linux）。
2. 下载本仓库全部部署文件到服务器或目标电脑。
3. 将 `.env.example` 复制为 `.env`，并修改下方配置。
4. Docker Hub 仓库为私有时，先执行 `docker login`。
5. Windows 双击 `start.bat`；Linux 执行 `chmod +x start.sh && ./start.sh`。

启动脚本会执行 `docker compose pull`，从 Docker Hub 拉取并启动：

- `lenglengyou/delta-game:tagname`
- `lenglengyou/delta-game-admin:tagname`
- `lenglengyou/delta-game-h5:tagname`
- `mysql:8.0`
- `redis:7-alpine`

启动后访问：

- 管理后台：`http://服务器IP:8081`
- H5 页面：`http://服务器IP:8082`

## 必须修改的配置

编辑 Docker 配置目录内的 `.env`，至少修改：

```env
MYSQL_ROOT_PASSWORD=强Root密码
MYSQL_PASSWORD=强数据库密码
REDIS_PASSWORD=强Redis密码
JWT_SECRET=至少32字符的随机字符串
AES_KEY=16字节AES密钥
```

每个部署实例的小程序 AppID 与 AppSecret 独立配置在同一 `.env` 文件：

```env
WX_MINIAPP_ENABLED=true
WX_MINIAPP_APPID=你的微信小程序AppID
WX_MINIAPP_SECRET=你的微信小程序AppSecret
```

修改小程序配置后，在配置目录执行：

```bash
docker compose up -d --force-recreate backend
```

可在 `.env` 中调整访问端口：

```env
ADMIN_PORT=8081
H5_PORT=8082
FILE_UPLOAD_URL_PREFIX=/file/
```

## 微信支付与域名

微信支付参数在管理后台“系统配置 -> 微信支付配置”中维护。商户私钥、公钥和上传文件会保存到 Docker 数据卷 `upload-data`。

生产环境使用 Nginx 或宝塔配置 HTTPS 反向代理：

```text
管理后台域名 -> 127.0.0.1:8081
H5 域名     -> 127.0.0.1:8082
```

H5 微信支付回调地址必须是公网可访问的 HTTPS 地址。

## 常用命令

以下命令都在 Docker 配置目录中执行：

```bash
# 拉取最新镜像并更新服务
docker compose pull
docker compose up -d

# 查看状态和后端日志
docker compose ps
docker compose logs -f backend

# 停止服务，保留数据
docker compose down
```

首次启动会导入 `delta_game.sql`。已有 MySQL 数据卷不会再次导入初始化 SQL。请勿执行 `docker compose down -v`，除非确认需要删除数据库、Redis 和上传文件。
